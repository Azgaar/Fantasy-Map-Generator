/** The trade animation every browser starts from. The one definition of these values */
export const DEFAULT_TRADE_ANIMATION = {
  displayType: "both" as const,
  concurrent: 30,
  duration: 250,
  landDurationModifier: 5,
  segmentChangePause: 1000,
  markerSize: 4
};
